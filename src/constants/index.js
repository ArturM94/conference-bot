const ERROR = require('./errors');
const { WELCOME_MSG } = require('./start');
const { HELP_MSG } = require('./help');
const DATABASE = require('./database');

module.exports = {
  ERROR,
  WELCOME_MSG,
  HELP_MSG,
  DATABASE,
};
